# Rule: App Router only

This project uses Next.js App Router exclusively.

- All pages live under `src/app/`
- Never create or reference `src/pages/`
- Route handlers go in `src/app/api/*/route.ts`
- Layouts go in `src/app/**/layout.tsx`
